<h1>public/homes#<%= action_name %></h1>

<div class="d-flex align-items-center p-3">
  <span class="page-title">ログイン</span>
</div>

<div class="col-12">
  <div class="d-flex align-items-center p-3">
    <span class="page-title">会員の方はこちらからログイン</span>
  </div>

  <div class="col-8 mx-auto">
    <%= form_with do |f| %>
      <div class="field mt-3">
        <%= f.label :email, "メールアドレス" %>
        <div class="col-8 px-0">
          <%= f.text_field :email, autocomplete: :true, autofocus: true, class: "col-12 px-1" %>
        </div>
      </div>
      <div class="field mt-2">
        <%= f.label :passward, "パスワード" %>
        <div class="col-8 px-0">
          <%= f.text_field :passward, autofocus: true, class: "col-12 px-1" %>
        </div>
      </div>

      <%# ja.ymlを利用してボタンの表示を分岐させる %>

      <div class="actions mt-3">
        <%= f.submit "Buttom", class: 'btn btn-sm btn-success' %>
      </div>
    <% end %>
  </div>


  <div class="d-flex align-items-center p-3 mt-5">
    <span class="page-title">会員登録がお済みでない方</span>
  </div>
  <p><%= link_to "こちら", root_path, class: "px-4" %>から新規登録を行ってください。</p>
</div>

<p>Find me in app/views/public/homes/<%= action_name %>.html.erb</p>